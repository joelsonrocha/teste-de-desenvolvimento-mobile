import React from 'react';
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
  } from 'react-native';

const Author = () => {
    return (
      <SafeAreaView>
        <Text>Author</Text>
      </SafeAreaView>
    );
  };

export default Author;